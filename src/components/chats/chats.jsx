import { useState } from "react";

const Chats = ({token, onChatSelect, selectedChat}) => {
    const [chats, setChats] = useState([])
	const [lastMessages, setLastMessages] = useState({})
}

export default Chats